export interface SocialLink {
  name: string;
  url: string;
  iconClass: string;
  color?: string;
}

export interface Particle {
  x: number;
  y: number;
  z: number;
}

export interface Vector3 {
  x: number;
  y: number;
  z: number;
}